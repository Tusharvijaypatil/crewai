# Frontend config. Copy to .env.local to override.
#
# true  (default) -> fully offline demo using the mock-data layer
# false           -> use the live FastAPI backend via the Vite /api proxy (:8000)
VITE_USE_MOCKS=true
